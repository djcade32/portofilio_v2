import Image from "next/image";

/**
 * Root page component that renders an empty <main> element.
 *
 * @returns The JSX element for the home page — an empty `<main>` element.
 */
export default function Home() {
  return <main></main>;
}